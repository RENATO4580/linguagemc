//Primeiro arquivo em linguagem C
//Vamos importa a biblioteca base para executar o nosso programa
//a biblioteca se chama stdio(std->Stardand | i -> input | o -> output)
//para importar, usaremos o comando #include
#include <stdio.h>

//Abaixo, iremos escrever os comandos que serão realmente executados
//utilizaremos a função main(principal) para iniciar a a execução
int main(){
    // O comando printf exibe uma mensagem em tela de console
    printf("Hello, World!\n");
    // Informa ao compilador(tradutor) gcc que o programa acabou
    return 0;
}