//primeiro arquivo em liguagem c 
//vamos importar a biblioteca base para executar o nosso programa 
//a biblioteca se chama stdio(std->standard | i -> input | o -> output)
//para importar, usaremos o comandos #include
#include <stdio.h>

//abaixo, iremos ecrever os comandos que serão realmente executados 
//utilizamos a função main(principal) para iniciar a execução 
int main(){
    // o comondo printf exibe uma mensagem em tela de console 
    printf ("Hello, World!\n");
    
    // Informa ao compilador (tradutor) gcc que o programa acabou
    return 0; 

}